name: snippetcodervideocall
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_webrtc_wrapper: ^1.0.2
  http: ^1.2.2
  shared_preferences: ^2.2.3
  snippet_coder_utils: ^1.0.15
  socket_io_client: ^2.0.3+1
  uuid: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
